<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:SBC.WPF.Models"
    xmlns:vm="using:SBC.WPF.ViewModels"
    x:DataType="vm:MainWindowViewModel">

    <DataTemplate x:Key="NavItemTemplate" DataType="models:TestGroup">
        <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ListBoxItem}}">
            <TextBlock
                FontWeight="Medium"
                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                Text="{Binding Name}" />
        </Border>
    </DataTemplate>


</ResourceDictionary>
